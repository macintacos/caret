#!/usr/bin/env bash
#MISE description="Smoke-test the run-from-source bundle: prewarm spawns a daemon that serves the UI"
#MISE depends=["build-bundle"]
# Thin forwarder: the temp package layout, prewarm, and HTTP probe live in the
# caret tasks CLI (scripts/tasks/cli.ts). "$@" is safe on an empty arg list (a
# special parameter, not a plain array — no set -u footgun).
exec bun scripts/tasks/cli.ts smoke-bundle "$@"
