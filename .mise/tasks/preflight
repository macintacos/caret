#!/usr/bin/env bash
#MISE description="Pre-push gate: lint + typecheck + svelte-check + test"
#MISE depends=["lint", "typecheck", "typecheck-ui", "test"]

set -euo pipefail

echo "preflight: lint + typecheck + typecheck-ui + test passed"
