#!/usr/bin/env bash
#MISE description="Smoke-test the compiled binary: serves the embedded multi-asset UI over the wire"
#MISE depends=["build"]
# Thin forwarder: the daemon boot, port discovery, and HTTP probe live in the
# caret tasks CLI (scripts/tasks/cli.ts). "$@" is safe on an empty arg list (a
# special parameter, not a plain array — no set -u footgun).
exec bun scripts/tasks/cli.ts smoke-bin "$@"
